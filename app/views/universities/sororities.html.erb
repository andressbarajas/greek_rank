<%= render partial: "shared/uni_category_panel", :locals => { category_panel_body: "sor_panel_body" } %>
<br>
<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="3">Sororities</th>
    </tr>
  </thead>

  <tbody>
    <% @chapters.each do |chapter| %>
      <tr>
        <td><%= link_to "#{chapter.sorority.name} - #{chapter.sorority.letters}", 
                        sorority_chapter_path(chapter) %></td>
    <% end %>
  </tbody>
</table>