<div class="spacer50"></div>
<div class="well well-sm">
  <h3 class="text-center">Recent Discussion</h3>
  <ul>
    <% @uni_discs.each do |disc| %>
      <li><%= link_to truncate_sidebar_link(disc.subject), topic_path(disc) %></li>
      <hr>
    <% end %>
  </ul>
</div>